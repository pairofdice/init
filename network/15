# 15. Make the intra.42.fr address reroute to 46.19.122.85
# the hosts file lists known hosts
# ip address and a host name pointing there is all you need for a redirection

1. open the /etc/hosts file with sudo
	sudo vim /etc/hosts
2. add the line
	46.19.122.85	intra.42.fr
3. save and exit
	:wq